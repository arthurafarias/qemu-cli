name: Deploy GitHub Pages

on:
  push:
    branches: [master]
    paths: [docs/**, lib/**, tests/**, pyproject.toml, .github/workflows/pages.yml]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install package and test dependencies
        run: pip install -e ".[test]" "genbadge[coverage]"

      - name: Run tests with coverage
        run: python -m pytest --cov-report=html:_site/coverage --cov-report=xml

      - name: Generate coverage badge
        run: genbadge coverage -i coverage.xml -o _site/coverage-badge.svg

      - name: Assemble site
        run: cp -r docs/. _site/

      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site
      - id: deployment
        uses: actions/deploy-pages@v4
